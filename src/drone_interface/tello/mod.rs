pub mod drone;

#[allow(dead_code)]
// This is acceptable because this is more like a library kind of thing, and we may not use all the packets we define, nor all the data in all the packets.
mod packet;
mod video_packet;

